#/* /index.html 200

#redirect api calls to actual http sources because of mixing error with https
/api/* http://numbersapi.com/:splat 200

#redirects to correct directories

/ztm-number-api/* /:splat 200

